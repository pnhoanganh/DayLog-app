import { Text, View } from "react-native";
import { FontFamily } from "../../constants/fonts";

const LaunchScreen = () => {
  return (
    <View>
      <Text style={{ fontFamily: FontFamily.Poetsen }}>Launch Screen</Text>
    </View>
  );
};

export default LaunchScreen;
